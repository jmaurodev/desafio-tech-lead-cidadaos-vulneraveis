name: 'cidadaos_vulneraveis'
version: '1.0.0'
config-version: 2

profile: 'cidadaos_vulneraveis'

model-paths: ["models"]
test-paths:  ["tests"]
seed-paths:  ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  cidadaos_vulneraveis:
    raw:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
